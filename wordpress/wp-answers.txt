1. The file that controls the homepage in a custom theme is index.php or front-page.php.
2. Use wp_enqueue_script() in functions.php to enqueue a custom JS file.
3. The function get_site_url() returns the base URL of the site.
